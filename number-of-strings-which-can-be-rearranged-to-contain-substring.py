from string import ascii_lowercase


class Solution:
    def stringCount(self, n: int) -> int:
        word = 'leet'
        def solve(i, f):
            if i < n:
                for ch in ascii_lowercase:
                    if ch in word:
                        


        return solve(0, {})
